# Hybrid Cloud Configuration
# Use this for AWS deployment that calls home GPU cluster

# In AWS ECS Task Definition or Lambda environment variables:

# Option 1: Cloudflare Tunnel (Recommended)
LLM_SERVICE_URL=https://llm.yourdomain.com
LLAMA_SERVER_URL=https://llm.yourdomain.com
LLAMA_API_TYPE=ollama

# Option 2: Tailscale VPN
LLM_SERVICE_URL=http://100.x.x.x:11430
LLAMA_SERVER_URL=http://100.x.x.x:11430
LLAMA_API_TYPE=ollama

# Option 3: AWS Client VPN
LLM_SERVICE_URL=http://10.0.1.100:11430
LLAMA_SERVER_URL=http://10.0.1.100:11430
LLAMA_API_TYPE=ollama

# Authentication (if enabled)
LLM_AUTH_TOKEN=your-secure-token-here

# Fallback configuration (if home cluster is down)
LLM_FALLBACK_ENABLED=true
LLM_FALLBACK_URL=http://backup-llm-service:5000

# Database (still in AWS RDS)
DATABASE_URL=postgresql://user:pass@your-rds-endpoint:5432/resume_db

# API Service settings
JWT_SECRET=your-jwt-secret
PORT=3000
NODE_ENV=production
