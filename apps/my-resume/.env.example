# Environment variables for React app (Rsbuild)
# In Rsbuild, environment variables must be prefixed with PUBLIC_ to be accessible in the browser

# LLM API URL - Update this to your home server's IP address
# LLM Service URL (Python Flask API)
PUBLIC_LLM_API_URL=http://localhost:5000

# Backend API URL (NestJS)
PUBLIC_API_URL=http://localhost:3000/api

# Example for production:
# PUBLIC_LLM_API_URL=http://your-home-server-ip:5000
