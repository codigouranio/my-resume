# Environment variables for React app (Rsbuild)
# In Rsbuild, environment variables must be prefixed with PUBLIC_ to be accessible in the browser

# Backend API URL (NestJS)
# Use relative path when behind nginx proxy, or full URL for direct access
PUBLIC_API_URL=/api

# LLM Service URL (Python Flask API)
# Use relative path when behind nginx proxy, or full URL for direct access
PUBLIC_LLM_API_URL=/llm

# Google Tag Manager ID (optional)
# Leave empty to disable GTM
PUBLIC_GTM_ID=GTM-XXXXXXX

# Google Analytics 4 Measurement ID (optional)
# Leave empty to disable GA4
PUBLIC_GA_ID=G-XXXXXXXXXX

# Example for development (direct access):
# PUBLIC_API_URL=http://localhost:3000/api
# PUBLIC_LLM_API_URL=http://localhost:5000

# Example for production with custom domain:
# PUBLIC_API_URL=https://yourdomain.com/api
# PUBLIC_LLM_API_URL=https://yourdomain.com/llm
